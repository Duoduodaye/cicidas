# Cicidas 仓库日常操作指南

## 🏠 你的仓库现状
- **仓库地址**: https://github.com/Duoduodaye/cicidas
- **网站地址**: https://cicidas.org
- **主要文件**: 
  - `index.html` - 主页
  - `ai-news-fixed.html` - AI新闻页面
  - `scripts/ai_news_fetcher.py` - 新闻抓取脚本
  - `scripts/update_ai_news.sh` - 自动化部署脚本

---

## 📅 日常操作场景

### 场景1：更新网站内容
**什么时候用**: 修改了HTML、CSS或其他网站文件

```bash
# 1. 进入项目目录
cd /Users/duoduodaye/cicidas

# 2. 查看有哪些文件被修改了
git status

# 3. 添加所有修改的文件
git add .

# 4. 提交修改（记得写清楚改了什么）
git commit -m "更新主页设计 - 添加了新的项目展示"

# 5. 推送到GitHub（网站会自动更新）
git push origin main
```

### 场景2：更新AI新闻数据
**什么时候用**: 想要获取最新的AI新闻

```bash
# 方法A：使用一键脚本（推荐）
cd /Users/duoduodaye/cicidas
./scripts/update_ai_news.sh

# 方法B：手动操作
cd /Users/duoduodaye/cicidas/scripts
python3 ai_news_fetcher.py    # 获取新闻
cp ai_news_data.js ../        # 复制到主目录
cd ..
git add ai_news_data.js
git commit -m "更新AI新闻数据"
git push origin main
```

### 场景3：添加新页面
**什么时候用**: 想在网站上新增页面

```bash
# 1. 创建新的HTML文件
touch new-page.html

# 2. 编辑文件内容（用你喜欢的编辑器）
# 3. 在index.html中添加链接

# 4. 提交新页面
git add .
git commit -m "添加新页面: 关于我的项目"
git push origin main
```

---

## 🆘 常见问题快速解决

### Q: 我修改了文件，但网站没变化？
```bash
# 检查是否真的推送了
git status
git log --oneline -5

# 如果没推送，执行推送
git push origin main

# 然后等2-3分钟，强制刷新浏览器(Cmd/Ctrl + Shift + R)
```

### Q: 忘记了Git命令怎么办？
```bash
# 万能三步走（99%的情况都能用）
git add .                           # 添加所有修改
git commit -m "更新网站 $(date)"     # 提交并自动生成日期
git push origin main                # 推送到GitHub
```

### Q: 想回到之前的版本？
```bash
# 查看历史提交
git log --oneline -10

# 回到某个提交（替换成实际的提交ID）
git checkout abc1234 -- filename.html

# 或者撤销最近一次提交
git reset --soft HEAD~1
```

---

## 🔧 实用命令备忘单

### 查看状态
```bash
git status              # 查看文件状态
git log --oneline -10   # 查看最近10次提交
git diff                # 查看具体修改内容
```

### 基本操作
```bash
git add filename.html   # 添加特定文件
git add .               # 添加所有文件
git commit -m "说明"    # 提交修改
git push origin main    # 推送到GitHub
```

### 紧急操作
```bash
git stash              # 临时保存修改
git stash pop          # 恢复临时保存的修改
git reset HEAD~1       # 撤销最近一次提交（但保留修改）
```

---

## 📱 移动端操作（手机/平板）

### 使用GitHub手机App
1. 下载 **GitHub Mobile** app
2. 登录你的账号
3. 找到 `cicidas` 仓库
4. 可以直接编辑小文件
5. 写提交信息并保存

### 使用网页版
1. 手机浏览器打开 https://github.com/Duoduodaye/cicidas
2. 点击要编辑的文件
3. 点击铅笔图标编辑
4. 提交修改

---

## 🎨 个性化定制建议

### 创建你自己的快捷脚本
```bash
# 创建personal-deploy.sh
cat > personal-deploy.sh << 'EOF'
#!/bin/bash
echo "🚀 开始更新我的网站..."

# 你的个性化操作
git add .

echo "请输入更新说明:"
read message
git commit -m "$message - $(date '+%Y-%m-%d')"

git push origin main
echo "✅ 网站更新完成！访问 https://cicadas.org"
EOF

chmod +x personal-deploy.sh
```

### 设置别名让命令更简单
```bash
# 添加到你的 ~/.bash_profile 或 ~/.zshrc
alias cici="cd /Users/duoduodaye/cicidas"
alias deploy="cd /Users/duoduodaye/cicidas && git add . && git commit -m 'Quick update' && git push"
alias status="cd /Users/duoduodaye/cicidas && git status"
```

---

## 🗓 建议的学习计划

### 第1天（今天）
- [ ] 试试查看git状态: `git status`
- [ ] 修改一个小文件，用三步走推送
- [ ] 验证网站确实更新了

### 第1周
- [ ] 熟练使用万能三步走
- [ ] 学会查看历史记录
- [ ] 尝试手动更新AI新闻

### 第2周  
- [ ] 学会使用自动化脚本
- [ ] 添加一个新页面
- [ ] 定制自己的快捷命令

### 第1个月
- [ ] 完全独立操作
- [ ] 能解决常见问题
- [ ] 开始考虑高级功能

---

## 🆘 求助模板

当你遇到问题时，可以这样问我：

```
我在操作 cicidas 仓库时遇到问题：

🎯 我想要: (比如：更新网站内容)
📝 我做了: (比如：修改了index.html，然后运行了git add .)
❌ 出现错误: (贴出完整的错误信息)
💻 系统: Mac/Windows
📷 截图: (如果可能的话)
```

---

记住：**你不是在学Git，你只是在学怎么更新自己的网站！** 🎉